import React from 'react';
// import {getParams, getCurrentPath} from 'react-micro-router';

export default function hello() {
    // const {path, params} = props.route;
    console.log('Hello');
    return (
        <div>
            <h1>Node Project Template</h1>
        </div>
    );
}
